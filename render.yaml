services:
  - type: web
    name: stocksync-api-anshmittal132
    runtime: docker
    rootDir: backend
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: stocksync-db
          property: connectionString
      - key: LOW_STOCK_THRESHOLD
        value: "5"
      - key: CORS_ORIGINS
        value: https://frontend-liard-three-30.vercel.app,https://frontend-hxrowwjbc-anshmittal04-5279s-projects.vercel.app

databases:
  - name: stocksync-db
    plan: free
    databaseName: stocksync
    user: stocksync
